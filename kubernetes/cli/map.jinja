# -*- coding: utf-8 -*-
# vim: ft=jinja

{#- Start imports #}
{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set kubectl = salt['pillar.get']('kubernetes:cli', default=defaults['kubectl'], merge=True) %}