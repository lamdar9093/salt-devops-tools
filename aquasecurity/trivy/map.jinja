# -*- coding: utf-8 -*-
# vim: ft=jinja

{#- Start imports #}
{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set trivy = salt['pillar.get']('aquasecurity:trivy', default=defaults['trivy'], merge=True) %}