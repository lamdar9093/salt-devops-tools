# -*- coding: utf-8 -*-
# vim: ft=jinja

{#- Start imports #}
{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set aws_nuke = salt['pillar.get']('aws:nuke', default=defaults['aws_nuke'], merge=True) %}