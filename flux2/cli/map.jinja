# -*- coding: utf-8 -*-
# vim: ft=jinja

{#- Start imports #}
{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set flux2 = salt['pillar.get']('flux2:cli', default=defaults['flux2'], merge=True) %}